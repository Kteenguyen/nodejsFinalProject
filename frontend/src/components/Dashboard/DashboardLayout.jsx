const DashboardLayout = () => {
    return (
        <div>
            <h1>Dashboard Layout</h1>
            {/* Add common dashboard layout elements like sidebar, header, etc. */}
        </div>
    );
}
export default DashboardLayout;